import React from 'react';
import './App.css';

function App() {
  const api = {
    key: process.env.API_KEY,
    base: process.env.API_BASE
  }

  return (
    <div className="App">
      
    </div>
  );
}

export default App;
